{
  "devDependencies": {
    "@types/cypress": "^0.1.6",
    "cypress": "^14.5.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^5.0.0",
    "mochawesome-report-generator": "^6.2.0"
  },
  "scripts": {
    "open:good": "npx cypress open --env configFile=good",
    "open:bad": "npx cypress open --env configFile=bad",
    "run:report": "npx cypress run --reporter mochawesome"
  }
}
